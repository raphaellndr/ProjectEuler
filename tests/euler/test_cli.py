"""Cli tests."""


# def test_help():
#     res = run(["python -m euler", "--help"], stdout=PIPE, stderr=PIPE, shell=True)
#     assert res.returncode == 0
#     assert res.stdout.startswith(b"Usage: python -m euler [OPTIONS] COMMAND [ARGS]...")
